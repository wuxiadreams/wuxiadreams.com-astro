---
import { AuthDialog } from "@/components/auth-dialog";

const user = Astro.locals.user;

if (user) {
  Astro.response.headers.set(
    "Cache-Control",
    "private, no-cache, no-store, must-revalidate",
  );
} else {
  Astro.response.headers.set(
    "Cache-Control",
    "public, max-age=60, s-maxage=31536000, stale-while-revalidate=60",
  );
}
---

{
  user ? (
    <a
      href="/library"
      class="inline-flex h-10 items-center justify-center rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground shadow-sm transition hover:bg-primary/90"
    >
      Go to my library
    </a>
  ) : (
    <AuthDialog
      client:only
      triggerText="Sign in / Start tracking"
      triggerClassName="inline-flex h-10 cursor-pointer items-center justify-center rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground shadow-sm transition hover:bg-primary/90"
    />
  )
}
